name: oran-functional-test-case

on:
#  pull_request:
#    branches:
#      - main
  workflow_dispatch:
    # For Branch-Protection check. Only the default branch is supported. See
    # https://github.com/ossf/scorecard/blob/main/docs/checks.md#branch-protection
    inputs:
      GERRIT_BRANCH:
        description: 'Branch that change is against'
        required: true
        type: string
#      GERRIT_CHANGE_ID:
#        description: 'The ID for the change'
#        required: true
#        type: string
#      GERRIT_CHANGE_NUMBER:
#        description: 'The Gerrit number'
#        required: true
#        type: string
#      GERRIT_CHANGE_URL:
#        description: 'URL to the change'
#        required: true
#        type: string
#      GERRIT_EVENT_TYPE:
#        description: 'Gerrit event type'
#        required: true
#        type: string
#      GERRIT_PATCHSET_NUMBER:
#        description: 'The patch number for the change'
#        required: true
#        type: string
#      GERRIT_PATCHSET_REVISION:
#        description: 'The revision sha'
#        required: true
#        type: string
#      GERRIT_PROJECT:
#        description: 'Project in Gerrit'
#        required: true
#        type: string
#      GERRIT_REFSPEC:
#        description: 'Gerrit refspec of change'
#        required: true
#        type: string

jobs:
  ftc-run:
    runs-on: ubuntu-22.04

    steps:
      # Step 1: Checkout the repository to build
      - name: Checkout Maven Project Repository
        uses: actions/checkout@v2
        with:
          repository: git@github.com:ravitejakarumuri/ccsdk-oran.git  # Replace with your Maven project repo
          ref: main
#          token: ${{ secrets.GITHUB_TOKEN }}      # Use GitHub token for access

      # Step 2: Set up JDK for Maven
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'adopt'  # Or 'zulu' or 'temurin'

      # Step 3: Run Maven build
      - name: Build with Maven
        run: |
          mvn clean install

      # Step 4: Build Docker image
      - name: Build Docker image
        id: build-image
        run: |
          docker build -t onap/ccsdk-oran-a1policymanagementservice:latest

      # Step 5: Run FTC Job
      - name: Run FTC job
       # working-directory: ${{ github.workspace }}/test/auto-test
        run: |
          git clone https://gerrit.o-ran-sc.org/r/nonrtric
          cd nonrtric/test/auto-test
          chmod +x ./verify-jobs-nonrtric.sh
          sudo bash ./verify-jobs-nonrtric.sh
